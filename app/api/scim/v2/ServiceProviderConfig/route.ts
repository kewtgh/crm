import { requireScim, scimFailure, scimJson } from "@/lib/scim-http";
export async function GET(request:Request){try{await requireScim(request);return scimJson({schemas:["urn:ietf:params:scim:schemas:core:2.0:ServiceProviderConfig"],patch:{supported:true},bulk:{supported:false,maxOperations:0,maxPayloadSize:0},filter:{supported:true,maxResults:100},changePassword:{supported:false},sort:{supported:false},etag:{supported:false},authenticationSchemes:[{type:"oauthbearertoken",name:"Bearer token",description:"Server-managed SCIM bearer token",specUri:"https://www.rfc-editor.org/rfc/rfc6750",primary:true}]});}catch(error){return scimFailure(error);}}
