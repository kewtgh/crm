import { supabaseJson } from "./supabase-server";
export type ProductRecord={id:string;nameZh:string;nameEn:string;code:string;price:number;billing:string;duration:string;durationEn:string;customers:number;revenue:number;active:boolean;isDefault:boolean;currency:string};
export async function listProducts():Promise<ProductRecord[]>{const [products,payments]=await Promise.all([supabaseJson<Record<string,unknown>[]>("/rest/v1/products?select=id,name_zh,name_en,code,billing_unit,duration_zh,duration_en,active,is_default,product_prices(currency,amount,effective_to)&order=is_default.desc,name_en"),supabaseJson<Record<string,unknown>[]>("/rest/v1/payments?select=product_id,amount,status")]);const stats=new Map<string,{revenue:number;customers:number}>();for(const payment of payments){if(payment.status!=="CONFIRMED"||!payment.product_id)continue;const id=String(payment.product_id);const current=stats.get(id)??{revenue:0,customers:0};current.revenue+=Number(payment.amount);current.customers+=1;stats.set(id,current);}return products.map((item)=>{const prices=(item.product_prices??[]) as Record<string,unknown>[];const price=prices.find((entry)=>entry.effective_to===null)??prices[0]??{};const stat=stats.get(String(item.id))??{revenue:0,customers:0};return{id:String(item.id),nameZh:String(item.name_zh),nameEn:String(item.name_en),code:String(item.code),price:Number(price.amount??0),billing:`products.billing.${String(item.billing_unit).toLowerCase().replace("school_year","schoolYear")}`,duration:String(item.duration_zh),durationEn:String(item.duration_en),customers:stat.customers,revenue:stat.revenue,active:Boolean(item.active),isDefault:Boolean(item.is_default),currency:String(price.currency??"CNY")};});}
export async function createProduct(input:{nameZh:string;nameEn:string;code:string;price:number;billing:string;duration:string;durationEn:string}){return supabaseJson<Record<string,unknown>>("/rest/v1/rpc/create_product_with_price",{method:"POST",body:JSON.stringify({product_code:input.code,product_name_zh:input.nameZh,product_name_en:input.nameEn,product_billing:input.billing,product_duration_zh:input.duration,product_duration_en:input.durationEn,price_currency:"CNY",price_amount:input.price})});}
export async function setProductActive(id:string,active:boolean){return supabaseJson<Record<string,unknown>[]>(`/rest/v1/products?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{Prefer:"return=representation"},body:JSON.stringify({active})});}
