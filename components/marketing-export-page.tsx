"use client";
import {useState} from "react";
import {FileLock2,ShieldCheck} from "lucide-react";
import {useI18n} from "./i18n-provider";
import {InlineMessage,Toast} from "./ui";
export function MarketingExportPage(){const{t}=useI18n();const[pending,setPending]=useState(false);const[error,setError]=useState("");const[toast,setToast]=useState("");const submit=async(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();setPending(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/marketing-exports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({channel:form.get("channel"),reason:form.get("reason")})});setPending(false);if(!response.ok){setError(t("marketingExport.blocked"));return;}setToast(t("marketingExport.submitted"));event.currentTarget.reset();};return <div className="page-stack"><section className="page-heading-row"><div><p className="eyebrow">{t("marketingExport.eyebrow")}</p><h1>{t("marketingExport.title")}</h1><p>{t("marketingExport.description")}</p></div></section><form className="surface consent-form" onSubmit={submit}><div className="surface-heading"><div><p className="eyebrow">{t("marketingExport.requestEyebrow")}</p><h2>{t("marketingExport.request")}</h2></div><FileLock2 size={22}/></div><label className="field"><span>{t("marketingExport.channel")}</span><select name="channel"><option value="EMAIL">Email</option><option value="SMS">SMS</option><option value="PHONE">{t("consent.channel.phone")}</option><option value="WECHAT">WeChat</option><option value="WHATSAPP">WhatsApp</option></select></label><label className="field"><span>{t("marketingExport.reason")}</span><textarea name="reason" rows={4} required/></label><InlineMessage type="info"><ShieldCheck size={16}/>{t("marketingExport.guard")}</InlineMessage>{error&&<InlineMessage type="error">{error}</InlineMessage>}<button className="primary-button" disabled={pending}>{pending?t("common.saving"):t("marketingExport.submit")}</button></form>{toast&&<Toast message={toast} onClose={()=>setToast("")}/>}</div>}
