# Lumina CRM v1.2.0 浏览器验收记录

> 已由 `work/browser-qa-chromium-1228/report.json` 取代。指定的本机
> `ms-playwright/chromium-1228` 已完成 23/23 真实检查；本文的环境阻断记录不再有效。

- 日期：2026-07-19
- 目标：生产构建 `v1.2.0`
- 状态：自动 HTTP 验收通过；真实浏览器矩阵因本机浏览器控制运行时冲突未形成通过证据。

## 已执行证据

- Vinext 生产构建成功并可在 3200 端口启动。
- base HTTP smoke 验证 liveness、JSON 认证错误、request ID、nonce CSP 和注册入口退役。
- Webhook HTTP smoke 验证可信 Origin、canonical signature、重放窗口、header 篡改拒绝和事件去重。
- 源码级无障碍复审确认 skip link、可见焦点、移动导航焦点约束、dialog/drawer Esc 与 Tab、menu 方向键/Home/End、combobox `aria-activedescendant` 和活动项滚动。
- 移动端关闭侧栏使用 `visibility:hidden`，防止离屏导航进入 Tab 顺序；顶栏与弹层共享高度 token。

## 未冒充通过的项目

浏览器控制进程被工作区外另一个 `type: module` Node 项目错误接管，启动阶段即退出。因此下列矩阵仍必须在可用浏览器运行时执行：

| 宽度 | 中文 | 英文 | 键盘/焦点 | 横向溢出 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1440px | 待实机 | 待实机 | 待实机 | 待实机 | 外部阻断 |
| 1024px | 待实机 | 待实机 | 待实机 | 待实机 | 外部阻断 |
| 375px | 待实机 | 待实机 | 待实机 | 待实机 | 外部阻断 |

## 实机操作清单

1. 登录、首次改密、MFA 与密码重置错误状态。
2. 跳至正文、全局搜索键盘选择、通知与个人菜单焦点恢复。
3. 手机侧栏打开/关闭、Tab 约束、Esc、背景滚动与遮罩。
4. 学校/联系人/任务编辑、并发冲突、历史、归档、导出审批。
5. 任务工作台批量完成、团队容量、保存个人/团队视图。
6. 导入多行引号 CSV、重复候选搜索、合并预览、确认和回滚。
7. 抽屉、菜单、表单错误、分页和中英文切换后无布局跳动。

浏览器矩阵完成前，不应声称 axe、视觉回归或实机 E2E 已通过。
