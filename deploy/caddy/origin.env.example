# Install as a root-readable Caddy environment file. The origin hostname is
# distinct from the public Cloudflare Worker hostname.
LUMINA_PUBLIC_HOSTNAME=crm.ewaya.com
LUMINA_ORIGIN_HOSTNAME=replace-with-distinct-origin.example.com
LUMINA_ORIGIN_AUTH_SECRET=replace-with-the-same-independent-worker-secret
