# Install as /etc/lumina-crm/caddy.env and load it from the Caddy systemd unit.
# This value must match the Cloudflare Tunnel public hostname and httpHostHeader.
LUMINA_PUBLIC_HOSTNAME=crm.example.invalid
