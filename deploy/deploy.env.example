# Deployment-only Supabase CLI credentials. Install as
# /etc/lumina-crm/deploy.env with root:lumina-crm ownership and mode 0640.
# Never copy this file into a release and never place these values in sudoers.
SUPABASE_ACCESS_TOKEN=replace-with-supabase-cli-access-token
SUPABASE_DB_PASSWORD=replace-with-production-database-password
SUPABASE_PROJECT_REF=ectxevxmcwzvwsjkwnld
